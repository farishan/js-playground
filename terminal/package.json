{
  "name": "terminal",
  "version": "0.1.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "node dev",
    "build": "rm -rf dist && node build"
  },
  "keywords": [],
  "author": "Faris Han",
  "license": "ISC",
  "dependencies": {
    "esbuild": "0.16.9"
  }
}
